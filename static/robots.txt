# Agent Baseline is a public reference document. It exists to be read, quoted
# and cited — by people, by search engines and by models. Nothing here is
# disallowed, and no crawler is singled out.

User-agent: *
Allow: /

# The catalogue this document renders from is published as a file, not only
# as a page: /controls.yaml is the source of truth and its identifiers are
# permanent. /llms.txt indexes it alongside the paper in markdown.

Sitemap: https://agentbaseline.org/sitemap.xml
